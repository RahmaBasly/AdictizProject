import { RechercherlivrePipe } from './rechercherlivre.pipe';

describe('RechercherlivrePipe', () => {
  it('create an instance', () => {
    const pipe = new RechercherlivrePipe();
    expect(pipe).toBeTruthy();
  });
});
